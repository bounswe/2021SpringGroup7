import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  InfoBoxContainer: {
    display: 'flex',
    flexDirection: 'column',
    padding: 8,
  },
  numberText: {textAlign: 'center', fontWeight: '500'},
  headingText: {
    fontWeight: 'bold',
  },
});
